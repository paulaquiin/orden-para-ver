{
  "name": "orden-para-ver",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "node scripts/build-blog.js",
    "dev": "vite",
    "dev:ai": "node scripts/build-blog.js && wrangler pages dev --compatibility-flag=nodejs_compat -- npx vite",
    "prebuild": "node scripts/build-blog.js",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.0.4",
    "wrangler": "^4.85.0"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "front-matter": "^4.0.2",
    "marked": "^18.0.2"
  }
}
